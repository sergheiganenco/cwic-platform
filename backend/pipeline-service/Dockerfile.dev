FROM node:18-alpine
WORKDIR /app

# Copy package.json only (workspace setup)
COPY package.json ./

# Install dependencies
RUN npm install --ignore-scripts --no-audit --no-fund || npm install --no-audit --no-fund

# Copy application code
COPY . .

EXPOSE 3004
CMD ["npm", "run", "dev"]
